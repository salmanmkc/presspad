import styled from 'styled-components';

// eslint-disable-next-line import/prefer-default-export
export const Wrapper = styled.div`
  width: 100%;
  min-height: ${({ theme }) => theme.spacings.FooterHeight};
  background-color: #00000026;
`;
