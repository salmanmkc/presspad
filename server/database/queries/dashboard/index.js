const internDashboard = require('./internDashboard');
const hostDashboard = require('./hostDashboard');
const orgDashboard = require('./orgDashboard');

module.exports = {
  internDashboard,
  hostDashboard,
  orgDashboard,
};
